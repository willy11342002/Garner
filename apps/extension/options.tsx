// 登入請直接在 Vela 網頁版操作，extension 會自動同步登入狀態
export default function Options() {
  return (
    <div style={{ padding: 24, fontFamily: "sans-serif", color: "#888" }}>
      <p>請在 Vela 網頁版登入，extension 會自動同步。</p>
    </div>
  )
}
