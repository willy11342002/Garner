# env
.venv
.env
.env.local
.env.production
.env.development

# Node
node_modules/
.nuxt/
.output/
dist/
build/

# Python
__pycache__/
downloads/
# flashrank 模型快取（約 162MB）。compose 把 ./apps/api 掛成 /app，
# 容器在 /app/.cache 下載的模型會直接寫回這裡。
.cache/
*.ipynb
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/

# Plasmo
.plasmo/
keys.json

# Claude Code
# settings.json（外掛宣告）要進版控；settings.local.json 有 API key，永遠不要
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db
