{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start DB",
      "type": "shell",
      "command": "docker compose up db -d",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "presentation": {
        "reveal": "silent",
        "panel": "shared"
      }
    }
  ]
}
